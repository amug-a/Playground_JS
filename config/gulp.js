var path = {
  // Declaring our distribution folder
  dist_dir: 'dist',
  // Declaring all files in our dist folder as our dist files
  dist_files: 'dist/**/*.*',

  views: {
    src: 'src/views/**/*.ejs'
  }
}